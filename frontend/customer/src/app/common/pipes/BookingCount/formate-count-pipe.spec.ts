import { FormateCountPipe } from './formate-count-pipe';

describe('FormateCountPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateCountPipe();
    expect(pipe).toBeTruthy();
  });
});